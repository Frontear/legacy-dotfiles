#!/bin/sh

if [ $EUID -ne 0 ]; then
    echo "Requires root."
    exit 1
fi

REPO_HEAD="/var/cache/dotfiles"

if [ ! -d "$REPO_HEAD" ]; then
    sudo git init --bare $REPO_HEAD
fi

git --git-dir=$REPO_HEAD --work-tree=/ $@
